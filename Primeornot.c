#include<studio.h>
#include<conio.h>
Void main()
{
int n,c=0;
Clrscr();
Print f ("enter any number \n");
Scanf("%d",&n);
For(i=1,I<n,I++);
{
If(n%i==0)
{
C++;
}
}
If (C==2)
{
Printf(it's prime ");
}
Else
{
Printf ("it's not prime ");
getch()
}
}
